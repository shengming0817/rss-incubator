[package]
name = "platform-authoring-smoke"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
rss-contract = { version = "=0.1.0", registry = "rss-candidate" }
rss-platform = { version = "=0.3.0", registry = "rss-candidate" }
rss-request-context = { version = "=0.1.0", registry = "rss-candidate" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync", "time"] }

[lints]
workspace = true
